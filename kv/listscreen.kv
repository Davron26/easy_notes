<ListScreen>:
    ScrollView:
        FloatLayout:
            size_hint_y: None
            GridLayout:
                padding: [50, 75, 50, 0]
                spacing: 50
                cols: 1
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        root.manager.transition.direction = 'left'
                        root.manager.current = 'notes'
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        app.root.transition.direction = 'left'
                        app.root.current = 'notes'
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        app.root.transition.direction = 'left'
                        app.root.current = 'notes'
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        app.root.transition.direction = 'left'
                        app.root.current = 'notes'
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        app.root.transition.direction = 'left'
                        app.root.current = 'notes'
                Button:
                    size_hint_y: None
                    size_y: 200
                    on_press:
                        app.root.transition.direction = 'left'
                        app.root.current = 'notes'
